<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".DBActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <EditText
            android:id="@+id/ptitleEditText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ems="10"
            android:hint="Въведете заглавие на мястото"
            android:inputType="text" />

        <EditText
            android:id="@+id/pinfoEditText"
            android:layout_width="match_parent"
            android:layout_height="94dp"
            android:ems="10"
            android:gravity="start|top"
            android:hint="Въведете описание"
            android:inputType="textMultiLine" />

        <Button
            android:id="@+id/addplaceBtn"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Добави мястото" />

        <ListView
            android:id="@+id/viewallEl"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:minHeight="400dp"
            android:background="@color/colorPrimaryDark"/>
    </LinearLayout>
</androidx.constraintlayout.widget.ConstraintLayout>